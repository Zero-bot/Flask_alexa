from pushbullet import Pushbullet
from com.helpers.ConfigParser import read_config


# key = read_config("PUSH BULLET", "key")
# account = Pushbullet(key)
# devices = account.devices
# account.push_note(title='a', body='pushed from api', device=devices[0])
# print(account.devices)

